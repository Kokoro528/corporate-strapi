'use strict';

/**
 * product-subcontainer service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::product-subcontainer.product-subcontainer');
